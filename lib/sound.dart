import 'package:audioplayers/audioplayers.dart';

// Ignore file, unfinished
class Sound {
  static final AudioCache _player = AudioCache(prefix: 'assets/audio/');

  ding() async {
    //await _player.play();
  }
}
